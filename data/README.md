# Data Directory

Place your PDF files in this directory. The MCP server will automatically discover and index all `.pdf` files found here on startup.

Supported: any standard PDF with extractable text (not scanned images).
